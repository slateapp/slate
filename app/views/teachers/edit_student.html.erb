<%= form_tag student_path(@student), method: :patch do %>
  <%= hidden_field(:teacher, :true) %>
  <%= select("cohort","id", Cohort.all.collect {|cohort| [ cohort.name, cohort.id ] }) %>
  <%= submit_tag 'Update Student' %>
<% end %>