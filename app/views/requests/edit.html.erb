<div class="indent">
  <h1>Editing post</h1>

  <%= form_for(@request) do |f| %>

  <p>
    <%= f.label :description %>
    <%= f.text_area :description %>
  </p>
  <p>
    <%= f.label :category %>
    <%= f.select :category, Category.all.map { |category| [category.name, category.id] }, :include_blank => "-- Select One --" %>
  </p>
  <p>
    <%= f.submit "Update" %>
  </p>
  <% end %>


  <%= link_to 'Back', requests_path %>
</div>