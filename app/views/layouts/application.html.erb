<!DOCTYPE html>
<html>
<head>
	<title>
		Slate
	</title>

	<%= csrf_meta_tags %>
	<%= stylesheet_link_tag 'application' %>

</head>
<body>
<%= render 'partials/header' %>
<% flash do |key, value| %>
  <div class="alert <%= flash_class(key) %>"><%= value %></div>
<% end %>

	<%= javascript_include_tag 'application' %>
  <h3><%=current_student.inspect %></h3>
	<%= yield %>
</body>
</html>